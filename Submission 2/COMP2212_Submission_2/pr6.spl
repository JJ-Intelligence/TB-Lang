-- Challenge 6

try {
    while (True) {
        a1 = pop(in(0));
        out(a1);
        a2 = pop(in(0));
        out(a2);
        b1 = pop(in(1));
        out(b1);
        b2 = pop(in(1));
        out(b2);
        b3 = pop(in(1));
        out(b3);
    };
} catch (StreamOutOfInputException) { 0 };
