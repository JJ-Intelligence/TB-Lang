-- Challenge 3

out(0);

-- While there's enough input: pop 2 from 'b' and ignore the first Int, then pop 1 from 'a'.
while (!isEmpty(in(0)) && hasElems(2, in(1))) {
    pop(in(1));
    out(pop(in(1)));
    out(pop(in(0)));
};
