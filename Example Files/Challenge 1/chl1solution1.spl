-- Challenge 1, solution 1
func f (as, bs) = {
    out(pop(as));
    out(pop(as));
    out(pop(bs));
};

while (hasElems(2, in(0)) && hasElems(1, in(1))) {
    f(in(None), in(1));
}
out(
+ :
-- jsdsdsdsd