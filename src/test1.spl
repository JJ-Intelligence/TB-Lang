
-- type solve ([Int]*, [Int]) -> None
-- func solve (*[], *bs) = None; 
-- func solve (*[a], *bs) = out(a); 
-- func solve (*(x:y:as), *(z:bs)) = {
--     out ([x,y,z]);
--     solve (as, bs);
-- };

-- setIn([1,0]);

-- as = in(0);
-- bs = in(1);

-- solve (as,bs);



type f (Int, Int*, Bool) -> Int;
func f (x, *y, b) = {
    out(x);
    out(*y);
    out(b);
};

x = f(4, &8, False);