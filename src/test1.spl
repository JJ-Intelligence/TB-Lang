-- Challenge 1, solution 1
-- func solve ([], bs) = return ([]);
-- func solve (x:y:as, z:bs) = x:y:z:(solve (as, bs));

-- as = [1,2,3,4];
-- bs = [5,6,7,8];
-- out (solve (as,bs));

x = 0;
y = [];
while (x<3) {
    x = x + 1;
    y = x:y
};
out (y);