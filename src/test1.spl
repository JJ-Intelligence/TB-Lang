-- Challenge 1, solution 1
func solve ([], ys) = return ([]);
func solve (x:y:xs, z:ys) = x:y:z:(solve (xs, ys));

as = [1,2,3,4];
bs = [5,6,7,8];
solve (as,bs);
out (solve (as,bs));


x = 10;
while (x>0) {
    x = x - 1;
    y = 1;
--    out (x);
};
