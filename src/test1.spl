
-- func solve (*(x:y:ys)) = {
--     out(x);
--     out(y);
--     out(*ys);
-- };

-- ys = in(0);

-- solve(ys);

-- Challenge 1, solution 1
func solve ([], bs) = None; 
func solve (*(x:y:as), *(z:bs)) = {
    -- out ([x,y,z]);
    solve (as, bs);
};

setIn([1,0]);

as = in(0);
bs = in(1);

solve (as,bs);
