y = 3;

if (y == 2) {
    x = 4;
} elif (y == 1) {
    x = 6;
} else {
    x = 5
};

y = 4 : 5 : 4 : x : [6, 8]; -- should work

z = [[4,5], [4]] : []; -- should work

q = [] : [[4,5]]; -- should work

-- v = [True] : [] : [[4,5]]; -- type error
