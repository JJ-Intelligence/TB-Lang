-- Challenge 1, solution 1

setIn([0,1,2]);

while (!isEmpty(in(0)) && !isEmpty(in(1)) && !isEmpty(in(2))) {
    a = pop(in(0));
    b = pop(in(1));
    c = pop(in(2));
    out([c, b, a, a+b, b+c]);
};