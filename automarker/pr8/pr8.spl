-- Challenge 8

try {
    while (True) {
        for ({ i = 0; sum = 0 }; i < 5; i++) {
            a = pop(in(0));
            b = pop(in(1));
            sum += (a - b);
            out([a, b])
        };
        out(sum);
    };
} catch (StreamOutOfInputException) { 0 };